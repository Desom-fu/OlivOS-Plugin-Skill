# -*- encoding: utf-8 -*-
"""
这个文件故意保留为空实现，用于告诉使用者：

1. 项目里预留了 function.py 这个业务模块位置。
2. 但本模板不在这里预先塞入任何函数，以免混淆 main.py、message.py、utils.py
   的职责边界。
3. 这里负责纯 Python 业务逻辑的实现，适合放一些本插件需要用到的特有函数的实现等。

也就是说：
main.py 负责事件入口；
message.py 负责消息解析与回复流程；
utils.py 负责公共能力；
function.py 是预留位，负责纯 Python 业务逻辑，用于特有函数的实现等。
"""


function_module_note = '本模板预留 function.py，但当前不封装任何函数。'
